

<div>
    <table class="table">
      <thead>
        <tr>
            <th>Name</th>
            <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% temp.each do |question| %>

        <tr>
            <td>
                <strong><%=question.question%></strong>
            </td>

            <td><%= link_to "Remove", add_question_to_current_test_path(@test,question) , remote: true, class: 'btn btn-danger'%></td>
        </tr>
        <%- end %>
      </tbody>

    </table>
</div>
